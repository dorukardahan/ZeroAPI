{
  "name": "zeroapi-router",
  "version": "3.6.0",
  "private": true,
  "description": "ZeroAPI — balanced benchmark-aware routing for OpenClaw",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
