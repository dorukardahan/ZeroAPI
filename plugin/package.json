{
  "name": "zeroapi-router",
  "version": "3.3.0",
  "private": true,
  "description": "ZeroAPI — benchmark-driven model routing for OpenClaw",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
