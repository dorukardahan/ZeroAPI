{
  "name": "zeroapi-router",
  "version": "3.0.0",
  "private": true,
  "description": "ZeroAPI — benchmark-driven model routing for OpenClaw",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
