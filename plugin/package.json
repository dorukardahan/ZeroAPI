{
  "name": "zeroapi",
  "version": "3.10.2",
  "private": true,
  "description": "ZeroAPI - transparent subscription-aware model and account routing for OpenClaw",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "install": {
      "clawhubSpec": "clawhub:zeroapi",
      "defaultChoice": "clawhub",
      "minHostVersion": ">=2026.5.2"
    },
    "compat": {
      "pluginApi": ">=2026.5.2",
      "minGatewayVersion": "2026.5.2",
      "notes": "Model/provider routing is fully compatible. Auth-profile/account routing requires a JSON-backed session store; non-JSON backends (e.g. SQLite) are detected and disabled gracefully, including the default per-agent SQLite backend introduced in OpenClaw 2026.7.x."
    },
    "build": {
      "openclawVersion": "2026.5.2",
      "pluginSdkVersion": "2026.5.2",
      "lastAuditedOpenClaw": "2026.7.1-2"
    }
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
